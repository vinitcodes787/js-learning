//conditional statements 

let mode = dark;
let colour

    if mode ==="dark"; {
       colour = "black";
    }

    if mode ==="light"; {
       colour = "white";
    }

    console.log(colour);

// if else statement 

#1:-
if (mode === "dark") {
   colour = "black" ;

} else {
if (mode === "light") {
   colour = "white";
}

console.log(colour);

#2:-
let age = 25;

if (age <= 18) {
    console.log("vote");

} else {
    console.log ("not vote");
} 
