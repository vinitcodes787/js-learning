//conditional statements 

let mode = dark;
let colour

    if mode ==="dark"; {
       colour = "black";
    }

    if mode ==="light"; {
       colour = "white";
    }

    console.log(colour);

// if else statement 

if (mode === "dark") {
   colour = "black" ;

} else {
if (mode === "light") {
   colour = "white";
}

console.log(colour);
